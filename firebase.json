{
	"hosting": {
		"public": "build/", // "public" is the only required attribute for Hosting
		"cleanUrls": true, // The cleanUrls attribute allows you to control whether or not URLs should include the .html extension
		"ignore": ["firebase.json", "**/.*", "**/node_modules/**"]
	}
}
